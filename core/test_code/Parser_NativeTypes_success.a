message M {
}

function isum(list<int> lst) -> int {
  return 0;
}

procedure drive(future<M> f, M m) {
}

@construct {
  bool b = false;
  client c = @no_one;
  double d = 3.14;
  int i = 42;
  string s = "Hello World";
  label l = #l;
  M m;
  // list<int> list;
  maybe<bool> mb = true;
  maybe<double> md;
  // future<M> fut;
}

#l {
}