// this MAKES a ton of sense, so... perhaps... I need to think about this?
public map<principal, int> my_map1;

@construct {
  // legit failure, but it masks the above thing as good.
  my_map1.nope();
}