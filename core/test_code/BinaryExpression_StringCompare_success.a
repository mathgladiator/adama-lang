test PrimaryTest {
  assert "x" == "x";
  assert ("x" != "y");
  assert !("x" == "y");
  assert "x" < "y";
  assert "x" <= "y";
  assert !("x" > "y");
  assert !("x" >= "y");
  assert "y" > "x";
  assert "y" >= "x";
  assert !("y" < "x");
  assert !("y" <= "x");
}
